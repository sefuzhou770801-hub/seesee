# 交付报告：片库存放位置可以更改，外置盘没接上时明确提示

## 结论

第 5 轮审查后改了设计：更改位置只做「复制、核对、切换」三步，全程不删除旧位置的任何文件。旧片库始终完整保留，所以中途退出、核对失败、queue.json 读不出来这几种情况，最坏的结果只是新位置多出一份没用的副本，不会丢视频。原先为自动删除源文件写的状态判断和补删代码全部删掉。

## 问题原因

片库目录原先写死成 `~/Movies/Replay`，`QueueStore` 两个初始化都会无条件建目录。视频放到外置盘后，只搬文件不够：`queue.json` 里存的是旧绝对路径，下载、字幕、摘要、问答也都按这个目录落盘。外置盘没挂上时，旧代码还会在 `/Volumes` 下造出同名空目录。

前五轮审查的阻塞问题几乎都出在同一处：搬移最后一步会自动删除旧位置的源文件，于是每个中途退出的时间点都要判断「现在删安全不安全」。修一个点，审查就找到下一个；第 5 轮查出 queue.json 读不出来时恢复逻辑会把它当成空队列，照样删掉源文件。

## 现在的流程

1. 准备（只读不写）：有下载在进行就拒绝；新旧位置相同就不做事；任一位置所在的卷未连接就拒绝；新旧位置互相包含（一个在另一个里面）就拒绝；读并解码 queue.json，失败就拒绝；枚举旧位置全部文件，出错就拒绝；新位置已有同名文件时内容相同就跳过，内容不同就整体中止。
2. 写搬移记录 `media-folder-move.inprogress`（与 queue.json 同目录，原子写入）。里面有旧位置、新位置、新位置目录是不是本次新建的、复制前记下的文件清单（只含本次要写进去的文件）、本次要新建的子目录、queue.json 备份文件名、原来的偏好值。
3. 复制清单里的文件到新位置。
4. 逐文件核对：大小一致且 SHA-256 一致。
5. 切换：备份 queue.json → 用 `remappedMediaPath` 改写三类路径（原子写入）→ 写偏好 `MediaFolderPath` → 删除搬移记录。删除搬移记录的那一刻才算切换完成。
6. 切换完成后，`QueueStore` 把旧位置记在偏好 `MediaFolderPreviousPath`，设置页显示一行「旧位置还留着一份」，附路径和「在访达中显示」按钮。用户在访达里把旧位置删空后，这一行不再显示，记录也清掉。

失败与中途退出只有一套处理：`MediaFolderMoveRecovery.rollBackPendingMove`。搬移当场失败时马上调用；进程中途退出的，下次启动在读偏好和队列之前调用。它按记录做这几件事：

1. 记录读不出来，或者 queue.json 读不出来、解码失败：无法判断，什么都不写、不删，记录保留，界面提示。
2. 备份存在且与当前 queue.json 不同（说明已被改写）：把备份原子写回 queue.json。
3. 偏好已指向新位置：改回记录里的原值。
4. 到这里应用已经按旧位置运行。然后只删记录清单里、本次写进新位置的文件，再删本次新建且已经空了的目录。新位置原本就有的文件一个不碰。
5. 新位置所在的卷没接上，或者有文件删不掉：记录保留，把备份名清空，挡住新的更改，界面提示。下次启动或下次更改位置时再清，此时不会再拿备份覆盖应用之后写的 queue.json。
6. 都清干净：删除记录，启动时提示「上次更改存放位置没有完成，仍在使用原来的位置」。

另外：`QueueStore.load()` 发现 queue.json 存在但读不出来或解码失败时，本次运行不再写 queue.json（原来会用空列表覆盖它），也拒绝更改片库位置，设置页显示「queue.json 读不出来，无法判断片库状态，没有改动任何文件」。

## 删了哪些代码

| 删掉的 | 原来做什么 |
|---|---|
| `MediaLibraryMover.deleteVerifiedSources`、结果 `finishedWithSourceLeftovers` | 切换后自动删除源文件，删不净时报「部分原文件没能清除」 |
| `MediaFolderMoveRecovery.inspect` / `apply` / `finishSourceCleanup` / `remainingSourceNames` | 重启时按四种判定补删源文件 |
| `allQueuePathsPointTo`、`destinationHoldsVerifiedCopies`、`correspondingSourcesGone`、`queueMediaFilesExist` | 判断「现在删源安全不安全」的四个条件 |
| `MediaLibraryMoveVerdict`（`idle` / `incomplete` / `committedNeedsCleanup` / `completedMarkerLeft`） | 恢复判定的四种状态 |
| `MediaFolderMoveMarker`（`isStaleCompleted`、`clearIfStaleCompleted`、`incompleteReason`、`from=` / `to=` 文本标记） | 读标记、判断标记是否过期、清标记 |
| `restoreQueue`、`restorePreference`、`rollback`、`clearMarkerIfPossible` | 当场失败的另一套回滚，和启动恢复是两份代码 |
| 中断钩子 `afterMarkerWritten`、`afterVerified`、`afterQueueBackedUp`、`afterSourceDeleteProgress`、`afterSourcesDeleted` | 状态表 a 到 j 里删源相关的节点 |
| `QueueStore.applyIncompleteMoveBannerIfNeeded`、`reconcileAfterFailedMove`、`moveMediaFolder` 里的标记分支 | 启动和再次更改时的补删与提示 |
| 文案 `sourceLeftovers`、`leftoverDestinationNeedsCleanup`、`rollbackLeftResidue`、`incompleteMove`、`needsManualCleanup` | 上面这些状态的提示 |
| 检查里状态表 a 到 j 的十个中断用例、补删与清标记的用例 | 对应上面删掉的代码 |

`MediaLibraryMover.swift` 从 761 行减到 502 行，其中恢复逻辑剩一个函数。`media_library_mover_check.swift` 从 1170 行减到 695 行。`move` 不再接收内存里的 `items`，改为自己读盘上的 queue.json，读不出来就拒绝。

## 保护靠什么

- 旧片库始终不删。代码里对旧位置只有读操作（枚举、读大小、算 SHA-256、复制时读），删除调用只出现在「删新位置里清单上的文件」「删本次新建的空目录」「删搬移记录」三处。
- 退回只删复制前记下的清单：清单在第一次复制之前写好，只含新位置原本没有的文件；记录里的相对路径是空的、绝对的或带 `..` 的一律不认；记录里新旧位置相同或互相包含时，不删任何东西。
- queue.json 改写前先原子写出备份，改写本身也是原子写入，所以任何时刻 queue.json 不是旧内容就是完整的新内容。
- 搬移记录在就说明没完成：退回时先恢复 queue.json 和偏好，再清新位置，所以即使清不掉，应用也按旧位置运行。
- queue.json 读不出来时一律当作无法判断：搬移拒绝、恢复不动、应用不覆盖。

## 修改范围

新增：

- `Sources/Replay/MediaFolderCopy.swift`：界面与状态文案
- `Sources/Replay/MediaFolderAvailability.swift`：按挂载卷列表判断未连接
- `Sources/Replay/MediaFolderPreference.swift`：偏好键 `MediaFolderPath`
- `Sources/Replay/MediaFolderLaunchArguments.swift`：启动参数 `--move-media-folder`
- `Sources/Replay/MediaLibraryMover.swift`：搬移记录、退回逻辑、搬移模块
- `Sources/Replay/DigestSettingsLiveView.swift`：设置窗跟随 `QueueStore`
- `tools/media_library_mover_check.swift`、`tools/media_folder_store_check.swift`、`tools/media_folder_move_safety_check.swift`、`tools/media_folder_settings_proof.swift`、`tools/media_folder_launch_move.sh`

改动：

- `QueueStore.swift`：`mediaFolder` 可变并发布；启动先退回未完成的更改；未连接不建目录、不开始下载；queue.json 读不出来时不写；切换后记下旧位置
- `DigestSettings.swift` / `DigestSettingsView.swift`：「更改…」、未连接提示、进度、失败说明、旧位置一行
- `ReplayApp.swift`：设置窗改走 `DigestSettingsLiveView`
- `ContentView.swift`：主窗口未连接横幅；就绪视频在未连接时播放区显示同样提示
- `scripts/test.sh`：加入四条检查

没有改 `~/Library/Application Support/Replay` 的位置，没有改字幕、摘要、问答的功能，没有动真实片库。

## 完整调用路径

1. 设置页「更改…」→ `QueueStore.presentMediaFolderPicker()` → `QueueStore.moveMediaFolder(to:)`。
2. 启动参数 `--move-media-folder <路径>` → 生产 `QueueStore.init()` 里 `scheduleLaunchMediaFolderMoveIfNeeded()` → 同一个 `moveMediaFolder(to:)`。
3. `moveMediaFolder` 暂停新下载，`flushPendingSaves` 后调用 `MediaLibraryMover.move`（流程见上）。
4. 成功：`QueueStore.mediaFolder` 更新并重新 `load()`，`ContentView`、摘要、问答都读 `store.mediaFolder`；`previousMediaFolder` 更新，设置页显示旧位置一行。
5. 失败：界面显示「搬移没有完成，原来的视频都还在：<原因>」。日志子系统 `com.mg.replay`，类别 `media-folder`。
6. 下次启动：`QueueStore.init()` 在读偏好之前调用 `rollBackPendingMove`，再按（可能已退回的）偏好定片库目录。

## 测试

### 任务书指定的数据安全检查：`tools/media_folder_move_safety_check.swift`

全部经真实 `QueueStore` 装配：更改用 `store.moveMediaFolder`，「下次启动」用同一数据文件重新构造 `QueueStore`。中断用生产代码里的 `MediaLibraryMover.interruptAfter` 钩子，到点直接抛出、不走失败清理，现场与进程在那一刻退出相同。每条单独记结果，最后汇总。

| 用例 | 内容 |
|---|---|
| `switch_keeps_every_old_file` | 切换成功后旧位置逐文件比对 SHA-256，一个不少、内容不变；新位置是完整副本 |
| `interrupt_during_copy` | 中断点 1：复制完第 2 个文件时退出，重启后旧位置原样、queue.json 与搬移前逐字节相同、偏好复原、新位置复制的文件和本次新建的目录都清掉、队列指向旧位置 |
| `interrupt_at_verify_failure` | 中断点 2：核对发现不一致的那一刻退出，重启后同上 |
| `verify_failure_in_process` | 核对失败但进程没退出：当场退回，结果同上 |
| `interrupt_during_queue_rewrite` | 中断点 3：queue.json 已改写成新路径、偏好还没写时退出，重启后同上（queue.json 从备份退回） |
| `interrupt_after_preference_saved` | 偏好已写、搬移记录还没删时退出，重启后同上 |
| `corrupt_queue_move_touches_nothing` | queue.json 解码失败时要求更改：拒绝；旧位置、queue.json、数据目录、偏好都不变，新位置没建，`flushPendingSaves` 也不覆盖 |
| `corrupt_queue_pending_move_touches_nothing` | 有未完成的更改、同时 queue.json 解码失败：重启后旧位置、新位置、queue.json、数据目录、偏好全部不变 |

#### 红：旧逻辑（提交 3a7404b）

同一份检查文件，只把 `.atVerifyFailure`（旧代码没有这个钩子）换成 `nil`，其余不改，与 `git archive 3a7404b Sources` 导出的旧源码一起编译运行（文件名里的编号每次运行不同）：

```
FAIL switch_keeps_every_old_file
  - 旧位置文件必须一个不少、内容不变：少了 ["39BDD479-….jpg", "39BDD479-….mp4", "39BDD479-….zh.srt", …, "notes/readme.txt"]，变了 []
FAIL interrupt_during_copy
  - 恢复完成后不留搬移记录
  - 新位置里本次复制的文件必须清掉，还剩 ["95006EB1-….mp4", "EAE5215D-….mp4"]
  - 本次新建的新位置目录应一并清掉
FAIL interrupt_at_verify_failure
  - 中断后应留下搬移记录，现场与进程退出一致
  - 核对失败时新位置应已有复制出的文件，现场才有意义
PASS verify_failure_in_process
FAIL interrupt_during_queue_rewrite
  - queue.json 必须回到搬移前的内容
  - 恢复完成后不留搬移记录
  - 新位置里本次复制的文件必须清掉，还剩 [全部 10 个文件]
  - 本次新建的新位置目录应一并清掉
  - 重启后队列应指向旧位置：…/new-library/FA77A15D-….mp4（三条）
FAIL interrupt_after_preference_saved
  - 旧位置文件必须一个不少、内容不变
  - queue.json 必须回到搬移前的内容
  - 偏好必须回到搬移前（未设置）
  - 新位置里本次复制的文件必须清掉，还剩 [全部 10 个文件]
  - 本次新建的新位置目录应一并清掉
  - 重启后队列应指向旧位置：…（三条）
FAIL corrupt_queue_move_touches_nothing
  - queue.json 读不出来时必须拒绝，实际 success
FAIL corrupt_queue_pending_move_touches_nothing
  - 旧位置文件不得有任何变化
  - 损坏的 queue.json 不得被覆盖
  - 数据目录不得有任何变化，搬移记录保留
media_folder_move_safety_check=failed 7/8
```

`verify_failure_in_process` 在旧逻辑上也通过：旧代码当场失败时同样会回滚。`interrupt_at_verify_failure` 在旧逻辑上没有对应钩子，红的原因是旧代码走了当场回滚、没有留下退出现场，不代表旧代码在这一点会丢文件。

#### 绿：新代码

```
PASS switch_keeps_every_old_file
PASS interrupt_during_copy
PASS interrupt_at_verify_failure
PASS verify_failure_in_process
PASS interrupt_during_queue_rewrite
PASS interrupt_after_preference_saved
PASS corrupt_queue_move_touches_nothing
PASS corrupt_queue_pending_move_touches_nothing
media_folder_move_safety_check=passed
```

### 其他检查

- `media_library_mover_check=passed`：成功后旧位置保留、改坏复制当场退回、同名不同内容中止、下载中拒绝、同目录不做事、新旧位置互相包含时拒绝（伪造这种记录时退回也不删）、源或目标未连接拒绝、源不存在、枚举出错、复制器抛错后能重试、queue.json 坏了不写任何文件；搬移记录只列本次要写的文件和目录；退回时新位置原本就有的文件留着；清不掉时记录保留、挡住新更改、重试不覆盖之后写的 queue.json；新位置卷没接上时先退回队列与偏好，接上后再清；记录读不出来什么都不动；切回旧位置不需要再复制、两次备份同一秒不互相覆盖。
- `media_folder_store_check=passed`：未连接不建目录、不启动下载；搬移后 `store.mediaFolder` 跟着变且旧文件还在；启动时退回并提示、之后可以重新更改；记录读不出来时提示并挡住更改；切换后显示旧位置一行，重新打开应用仍显示，删空后不再显示；设置页收到的失败文案与 store 一致。
- `media_folder_settings_proof=passed`，三张图：
  - 正常，带「更改…」：`/tmp/media-folder-settings-connected.png`
  - 未连接提示：`/tmp/media-folder-settings-disconnected.png`
  - 切换后，带「旧位置还留着一份」和「在访达中显示」：`/tmp/media-folder-settings-switched.png`

### 全套检查

`scripts/test.sh` 遇到第一个失败就停，所以复制一份改成失败后继续、最后列出失败项（临时副本不入库），经 `~/.claude/bin/buildq` 运行：

```
FAILED_ITEMS: url_intake:run digest_hover_probe:run sidebar_slideout:run
```

三项都是任务书列出的主干原有失败（issue #9、#7）。其余全部通过，含 `media_library_mover`、`media_folder_store`、`media_folder_move_safety`、`media_folder_settings_proof`、`qa_remove`、`digest_settings`。

### 发布构建与启动参数实测

```
SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX26.sdk \
  ~/.claude/bin/buildq -- swift build --scratch-path <临时目录> -c release --product Replay
# Build complete! (44.44秒)
bash tools/media_folder_launch_move.sh <临时目录>/release/Replay
```

```
=== 成功搬移 ===
success dest=/tmp/seesee-launch-move.bHEval/dest-ok backup=…/queue.json.bak-20260925-152734 pref=/tmp/seesee-launch-move.bHEval/dest-ok marker=cleared source_kept=9/9
=== 冲突失败回滚 ===
fail rollback source_kept dest_conflict_untouched marker=cleared pref_unchanged
media_folder_launch_move=passed work=/tmp/seesee-launch-move.bHEval
```

临时家目录（`CFFIXED_USER_HOME`）里 3 个视频、3 个缩略图、3 条字幕：新位置 9 个文件 SHA-256 与原文一致，旧位置 9 个文件逐个比对仍在且不变，queue.json 全部可持久字段对照通过，备份存在，搬移记录已删。冲突失败路径：旧位置、queue.json、偏好都不变，新位置没有残留。实测前后 `com.mg.replay` 域里都没有 `MediaFolderPath`，脚本结束后没有残留的 Replay 进程。

## 没验证的部分

- 没有在运行中的应用里用鼠标点「更改…」「在访达中显示」；设置页只做了离屏截图和 store 到设置页模型的发布检查。
- 没有接上、拔掉真实外置盘；未连接判断和「新位置卷没接上时的退回」靠注入挂载列表覆盖。
- 没有对约 2.4 GB 的真实片库计时。搬移仍在主线程同步执行（前几轮就是这样，本轮没改），复制和核对期间界面会停住，进度文字可能来不及刷新。真实搬库前建议先评估是否要改到后台执行。
- queue.json 读不出来时应用本次运行不写队列，新加的视频不会保存；提示只在设置页显示，主窗口没有。这是按「一律当作无法判断」做的取舍。
- 设置页片库那一行原有按钮叫「打开」，新加的旧位置一行按任务书写成「在访达中显示」，两个按钮做的是同类事情但名字不同，需要主 Agent 定用哪一个。
- 更改位置时新位置的上级目录如果不存在，会一并建出来；退回只删新位置目录本身，不删这些上级空目录。
