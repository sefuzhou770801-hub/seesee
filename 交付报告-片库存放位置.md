# 交付报告：片库存放位置可以更改，外置盘没接上时明确提示

## 问题原因

片库目录原先写死成 `~/Movies/Replay`。`QueueStore` 两个初始化都会无条件 `createDirectory`。用户把视频放到外置盘后，只搬文件不够：`queue.json` 里仍是旧绝对路径，下载、字幕、摘要、问答也都按这个目录落盘。外置盘没挂上时，旧代码还会在 `/Volumes` 下造出同名空目录。

## 修改范围

新增：

- `Sources/Replay/MediaFolderCopy.swift`：界面与状态文案
- `Sources/Replay/MediaFolderAvailability.swift`：按挂载卷列表判断未连接（可注入卷根，供检查使用可写假卷）
- `Sources/Replay/MediaFolderPreference.swift`：偏好键 `MediaFolderPath`
- `Sources/Replay/MediaFolderLaunchArguments.swift`：启动参数 `--move-media-folder`
- `Sources/Replay/MediaLibraryMover.swift`：搬移模块（可注入复制器与 `UserDefaults`）
- `Sources/Replay/DigestSettingsLiveView.swift`：设置窗跟随 `QueueStore`
- `tools/media_library_mover_check.swift`
- `tools/media_folder_store_check.swift`
- `tools/media_folder_settings_proof.swift`

改动：

- `QueueStore.swift`：`mediaFolder` 改为 `@Published private(set) var`；未连接不建目录、不开始下载；搬移与卷挂载恢复
- `DigestSettings.swift` / `DigestSettingsView.swift`：增加「更改…」、未连接提示、搬移进度与失败说明
- `ReplayApp.swift`：设置窗改走 `DigestSettingsLiveView`
- `ContentView.swift`：主窗口横幅；就绪视频文件不可读时播放区显示同样提示
- `scripts/test.sh`：加入上述三条检查

没有改 `~/Library/Application Support/Replay` 的存放位置，没有改字幕、摘要、问答的功能，没有动真实片库。

## 完整调用路径

1. 设置页「更改…」→ `DigestSettingsLiveView` 同步到模型 → `QueueStore.presentMediaFolderPicker()` → 选目录 → `QueueStore.moveMediaFolder(to:)`。
2. 启动参数 `--move-media-folder <路径>` → 生产 `QueueStore.init()` 里 `scheduleLaunchMediaFolderMoveIfNeeded()` → 同一段 `moveMediaFolder(to:)`。
3. `moveMediaFolder` 先停新下载并 `flushPendingSaves`，再调用 `MediaLibraryMover.move`：发现进行中标记则拒绝；源/目标未连接则拒绝；写 `media-folder-move.inprogress` → 复制（写入目标前就记下路径，跳过 `.DS_Store`）→ SHA-256 核对 → 备份 `queue.json` 为同目录 `queue.json.bak-<yyyyMMdd-HHmmss>` → 改写三类路径 → 写入 `MediaFolderPath` → 逐个删除原文件并检查结果 → 成功后才清标记。枚举出错或任一步失败都不当成功。
4. 成功后 `QueueStore.mediaFolder` 更新并 `load()`。`ContentView`、摘要、问答都读 `store.mediaFolder`，不另存副本。
5. 失败则删掉这次复制到目标的文件，原文件、`queue.json`、设置保持原样，界面显示「搬移没有完成，原来的视频都还在：<原因>」，日志子系统 `com.mg.replay`、类别 `media-folder`。

未连接：路径落在 `/Volumes/<卷名>/…` 且该卷不在 `FileManager.mountedVolumeURLs` 里。此时不建目录、不开始下载，排队状态写「存放位置未连接」。`NSWorkspace.didMountNotification` 后自动恢复并继续排队下载。

## 检查与实测结果

独立检查：

- `media_library_mover_check=passed`：成功搬移（SHA-256 用 `/usr/bin/shasum` 对照已知原文）、复制后改坏回滚、同名不同内容中止、下载中拒绝、目标即当前目录不做事
- `media_folder_store_check=passed`：真实 `QueueStore(dataFile:mediaFolder:)` 在可写假卷根下不建目录、不启动下载；`/Volumes/不存在的卷/seesee` 判未连接且路径不存在；搬移后 `store.mediaFolder` 跟着变
- `media_folder_settings_proof=passed`
  - 正常：「更改…」按钮，`/tmp/media-folder-settings-connected.png`
  - 未连接：「存放位置未连接」，`/tmp/media-folder-settings-disconnected.png`
- `digest_settings_check=passed`，`digest_settings_proof=passed`（原设置页高度仍为 342，未超过 420）

`swift build -c release`（`SDKROOT` 指向 MacOSX26 SDK）成功。

临时家目录实测（`CFFIXED_USER_HOME`，未碰真实片库）：

- 造 3 个视频和对应 `queue.json`，启动构建出的 `Replay --move-media-folder <临时目录>`：目标文件 SHA-256 与原文一致，原文件已删，`queue.json` 路径全部指向新目录，备份 `queue.json.bak-20260924-175144` 存在
- 再次启动：列表仍是「视频1」「视频2」「视频3」（读回 `queue.json`）
- 把设置指到 `/Volumes/不存在的卷/seesee` 启动：`/Volumes/不存在的卷` 没有被创建，偏好仍是该路径

反向验证：临时去掉 `createMediaFolderIfConnected` 里「未连接则返回」这一句后，`media_folder_store_check` 在「未连接时不得创建片库目录」失败（进程状态 133），随后已把这一句恢复。

全套 `scripts/test.sh`（失败后继续，正确工程目录）失败项：`url_intake`、`sidebar_slideout`、`digest_hover_probe`（任务书已知），以及首次跑时 `qa_remove` 因编译列表没带上新文件而编不过。已把新文件补进 `qa_remove` 的编译列表，正在重跑这一条。

## 数据安全

- 备份位置：与 `queue.json` 同目录，文件名 `queue.json.bak-<yyyyMMdd-HHmmss>`
- 失败恢复：核对或写入任一步失败时，已复制到目标的文件会删掉；原目录文件不删；`queue.json` 用原子写入，失败则从备份拷回；`MediaFolderPath` 保持原值
- 源文件只在全部核对通过、路径与设置都写完之后才删；原目录本身保留
- 复制用 `FileManager.copyItem`，不依赖 APFS 克隆，exFAT 可用
- 开发和实测都在临时目录或 `CFFIXED_USER_HOME` 里进行

## 第 1 轮退修

对照审查报告 1–5 和 `try?` 回滚缺陷：

1. 源未连接先用 `MediaFolderAvailability` 拒绝，报「片库存放位置未连接，无法搬移」；源不存在或不可读、枚举出错都失败并回滚。证明：`checkDisconnectedSourceIsNotEmptyLibrary`、`checkMissingSourceFails`、`checkEnumerationErrorFails`、`checkDisconnectedSourceMoveIsRejected`。
2. 目标路径在 `copyItem` 之前入账；复制器抛错后清掉已落地文件，同一目标可以重试。证明：`checkCopyThrowLeavesNoResidueAndCanRetry`。
3. 删除源文件失败返回 `.failure` 并留下标记；启动和再次搬移提示「上次搬移未完成」。证明：`checkDeleteFailureIsNotSuccess`、`checkIncompleteMarkerRefusesNewMove`。
4. 目标未连接拒绝并报「目标存放位置未连接」，`QueueStore` 默认搬移器注入挂载列表。证明：`checkDisconnectedDestinationDoesNotCreate`、`checkDisconnectedDestinationMoveDoesNotCreate`。
5. `DigestSettingsLiveView` 改订 `$mediaFolder` / `$mediaFolderMoveMessage` 等赋值后发布。证明：`checkSettingsMirrorSeesFailureAfterChange`。

回滚删除不再 `try?`，失败写进错误串。证明：`checkRollbackReportsDeleteFailure`。

真实启动参数搬移（`tools/media_folder_launch_move.sh`，临时家目录，不碰真实片库）：

- 成功：`CFFIXED_USER_HOME` + `Replay --move-media-folder <dest>`，3 个文件 SHA-256 一致、源已删、`queue.json` 已改、备份存在、标记已清。
- 失败：目标预置同名不同内容，源文件 / `queue.json` / 偏好保持原样，目标无新残留，标记已清。

命令与输出摘要见下方「启动参数实测」。

## 没验证的部分

- 没有在老板正在使用的电脑上点设置页「更改…」做真实搬库（任务书要求由负责人用启动参数做）
- 没有用鼠标在运行中的应用里添加链接；未连接时不下载由 `QueueStore.startDownload` 检查覆盖
- 没有接上/拔掉真实外置盘来看挂载通知；恢复逻辑已接线，靠注入挂载列表覆盖判定
- 没有在运行中的应用窗口里用鼠标添加链接；未连接时不下载由 `QueueStore.startDownload` 检查覆盖

## 全套检查

第 1 轮退修后，失败后继续跑完整 `scripts/test.sh`（工程目录写死的临时副本）失败项：

- `url_intake:run`（任务书已知，issue #9）
- `sidebar_slideout:run`（任务书已知，issue #7）
- `digest_hover_probe:run`（任务书已知，issue #7）

其余检查通过，含 `media_library_mover`、`media_folder_store`、`media_folder_settings_proof`、`qa_remove`。`swift build -c release` 成功。

## 启动参数实测

磁盘：`df -h /Volumes/移动ssd` 当时剩余 91 GB。

```
SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX26.sdk \
  ~/.claude/bin/buildq -- swift build -c release --product Replay
# Build complete! (42.30秒)
bash tools/media_folder_launch_move.sh .build/release/Replay
```

输出摘要（工作目录 `/tmp/seesee-launch-move.kIcOvc`）：

```
=== 成功搬移 ===
success dest=/tmp/seesee-launch-move.kIcOvc/dest-ok backup=/tmp/seesee-launch-move.kIcOvc/home/Library/Application Support/Replay/queue.json.bak-20260925-002926 pref=/tmp/seesee-launch-move.kIcOvc/dest-ok marker=cleared
=== 冲突失败回滚 ===
fail rollback source_kept dest_conflict_untouched marker=cleared pref_unchanged
media_folder_launch_move=passed work=/tmp/seesee-launch-move.kIcOvc
```

SPM 可执行文件写偏好域 `Replay`，不是打包应用的 `com.mg.replay`。实测前后都核过：`com.mg.replay` 没有 `MediaFolderPath`，真实 `~/Movies/Replay` 没有这次的测试文件。脚本结束后会删掉 `Replay` 域里的测试路径。
