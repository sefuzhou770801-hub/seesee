# 交付报告：队列来源换成真实品牌标志

## 改了什么

只改了 `Sources/Replay/SourceBrandMark.swift`。

- `youtubeMark`：不再用灰色圆角矩形加系统符号 `play.fill`。改成官方红色 `#FF0000` 圆角矩形（高度 10、圆角 1.8，约 18%）加白色播放三角。三角三个点按官方标志相对矩形的比例放到现有 `14×10` frame 里。
- `xMark`：不再用两条圆头直线画 X。改成 Simple Icons 的官方字标路径：外轮廓 12 个点 + 反向缠绕的内部 8 个点，按 `0.5` 缩进现有 `12×12` frame，用默认 nonzero `.fill(OpenMyChrome.ink)` 挖出交叉处的镂空缝隙。
- 文件头补了来源和许可：Simple Icons 16.32.0，CC0-1.0。
- `bilibiliMark`、`xiaohongshuMark`、`unknown` 分支、`SourceBrandMark(sourceName:)` 对外接口都没动。

## 为什么这样改

老板要的是真实品牌标志和官方颜色，覆盖旧的「队列里只用单色、不要品牌色」取舍。仓库没有图片资源管线，这两个标志按既有惯例用 SwiftUI `Path` / `RoundedRectangle` 手绘，不引入 `.xcassets` 或 SVG 解析库。

X 字标中间那道缝隙是官方标志的一部分。两个子路径按任务书给的顶点顺序描，默认填充规则就能挖洞；离屏放大后能看见缝隙，没有落到「填成实心 X」的后备方案。

## 构建与检查结果

`swift build -c release` 通过（42.20 秒，exit 0）。全量检查用 `/tmp/test-continue.sh`（`scripts/test.sh` 的失败后继续副本，不入库）：除主干原有三项外全部通过。

失败项只有任务书点名的三项，与本改动无关：`url_intake`（#9）、`digest_hover_probe`（#7）、`sidebar_slideout`（#7）。没有新失败。

## 截图路径

离屏渲染，深色画布，图标放大 12 倍看细节；另有队列行尺寸的对照图。改前、改后各渲一遍，哔哩哔哩和小红书四张图 `cmp` 字节一致。

近距特写（改后）：

- `交付截图-真实图标/youtube-closeup.png`
- `交付截图-真实图标/x-closeup.png`
- `交付截图-真实图标/bilibili-closeup.png`
- `交付截图-真实图标/xiaohongshu-closeup.png`

队列行尺寸：

- `交付截图-真实图标/youtube-row.png`
- `交付截图-真实图标/x-row.png`
- `交付截图-真实图标/bilibili-row.png`
- `交付截图-真实图标/xiaohongshu-row.png`

改前对照（不入库，留在 `/tmp/seesee-source-icons-before/`）。

目视结论：YouTube 是红底白三角，不再是灰底系统播放符号；X 是近白色字标，交叉处有镂空；哔哩哔哩、小红书与改前相同。

## 没验证的部分

- 没有打开已安装的 seesee.app 点真实队列。离屏图用的是同一个 `SourceBrandMark`，并按 `ContentView` 队列元数据行的字号和间距排了对照行。
- 没有浅色模式（应用写死深色）。

## 构建记录

- 基点：`ce595c1`（`fix/real-source-icons` 与 `origin/main` 相同）
- `~/.claude/bin/buildq -- swift build -c release`（`SDKROOT=/Library/Developer/CommandLineTools/SDKs/MacOSX26.sdk`）：排队 7836 秒后拿到槽位，`Build complete! (42.20秒)`，exit 0。链接器有两条既有警告：`CommandLineTools` 的 `usr/lib` 和 `Frameworks` 搜索路径不存在，与本改动无关。
- `~/.claude/bin/buildq -- /tmp/test-continue.sh`：排队 7881 秒后拿到槽位。失败项 3 个：`url_intake`、`digest_hover_probe`、`sidebar_slideout`。其余检查均为 `PASS`。

## 自检（同一会话顺序检查，不代替独立审查）

固定点 `ce595c1`，工作区 diff 为 `SourceBrandMark.swift`、交付报告和离屏截图。规格来源：`任务书-真实图标.md`。仓库标准只有 `CONTRIBUTING.md`（聚焦、解释可见行为、提交前跑 release 构建和 `test.sh`）。

### Standards

无确定违规。`xLogoPath` 是把官方 24×24 顶点收在一处再缩放，不是多余抽象。哔哩哔哩 / 小红书未改，没有重复开关或跨文件修改。

### Spec

任务要求的 YouTube / X 替换、接口不变、不动另外两个标志、不用资源目录或第三方库，都已落地。离屏图已核对镂空和红色。release 构建通过；全量检查除已知三项外通过。
