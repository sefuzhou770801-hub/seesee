# 探索：跳赞助段精华模式

## 程度

可玩。YouTube 来源在播放中会按 SponsorBlock 的 `sponsor` / `selfpromo` 区间自动跳到段尾，中央弹出与音量 HUD 同款提示（例如「已跳过赞助段 45 秒」）。开关在倍速菜单右侧，默认打开，全局记住。非 YouTube 不请求、不显示开关。

纯函数已进 `./scripts/test.sh`（videoID 提取、进入区间即跳、同段本轮不重复、相交区间合并、坏 JSON 静默、偏好默认开）。真实 API 响应（`aQp-iM0bCs4`，`[0, 31.861]`）已用同一解析器过一遍。工作树构建了 `dist/Replay.app`，未写入 `/Applications`，未改既有队列与媒体。

本会话没有在播放器里真跳一段：往队列加片会触发整段下载，落进用户媒体目录。逻辑与网络侧已验证，画面跳过仍待老板用下面路径试。

## 试玩路径

1. 打开本工作树的 `dist/Replay.app`（不要用 `/Applications/seesee.app`）。
2. 粘贴一条带赞助段的 YouTube 链接，例如 `https://www.youtube.com/watch?v=aQp-iM0bCs4`（片头约 0–32 秒是赞助）。等下载完成后从 0 播放。
3. 走到段头应直接跳到段尾，画面中央短暂出现「已跳过赞助段 N 秒」。
4. 倍速右侧的前进钮：亮着是开，再点即关。关掉后同一段不再自动跳。
5. 换一条哔哩哔哩或本地以外的非 YouTube：开关消失，网络里不应出现 `sponsor.ajay.app`。
6. 试完删掉自己加的那条，不要动原来的队列。

## 自评

- 跳过判定与 videoID 提取是纯函数，和播放器 observer 之间有清楚边界。
- 失败静默：无网、404、坏 JSON 都当没有区间。
- 同段本轮只跳一次，避免 seek 循环；用户 seek 回已跳过的段可以再看。
- 未做 intro / outro / 互动提醒；未做哈希匿名请求；未缓存到磁盘。
- 0.25 秒观察粒度会漏掉更短的区间。相交区间会合并成一次跳到最远尾，避免连续弹两次 HUD。续播 seek 未完成前不会抢跳赞助段。

## 建议

**留。** 看 YouTube 长视频时这是真能省时间的功能，接入面小，关掉即无感。

若转正，建议按这个顺序补：

1. 老板试玩确认：片头赞助、片中插入、关掉再看、非 YouTube 安静。
2. 开关文案和图标是否一眼能懂（现在用 `forward.end`，可能被当成「下一章」）。
3. 要不要把 intro / outro 做成第二档，还是永远只跳赞助和自推。
