# 探索：youtube-digest 式三分页侧栏

探索分支 `explore/digest-sidebar`。试玩构建在工作树 `dist/Replay.app`，未写入 `/Applications`，未改用户 `queue.json` 与媒体。

## 做了什么

右栏由「章节 / 字幕」两页改成「字幕 / 总览 / 笔记」三页，视觉沿用 watchGlass（深底、发丝边、胶囊页签）。

1. **字幕页**：顶部搜索框，字面匹配、不分大小写，命中句高亮，上一个 / 下一个在命中间跳转（不改播放位置）。点时间或点句仍跳播放。选中文字后出现「解释」「存笔记」。
2. **总览页**：一键把带 `[M:SS]` 时间戳的全文字幕发给 `claude-sonnet-5`，产出覆盖全片的章节加 3 到 5 条金句，点击跳转。结果写入该视频旁路文件，再次打开不重复调用。无生成结果时，视频自带章节仍可点。
3. **笔记页**：字幕里存下的时间戳笔记，点击跳转，可删除。逐视频旁路文件持久化。
4. **无密钥**：总览按钮与解释旁显示「未配置密钥」，探索版不做引导窗口。

模型调用复用 WatchQA 的密钥解析（`AnthropicAPIKey` / `ANTHROPIC_API_KEY`）和同一模型、同一接口。WatchQA 现有路径未改。

旁路文件（媒体目录下，删视频时随 `<uuid>.*` 前缀扫描清掉）：

- `{uuid}.digest.json` 总览缓存
- `{uuid}.notes.json` 笔记

旧的 `sidePaneMode=chapters` 会读成总览页。有自带章节的视频打开时仍先进总览（里面先看到自带章节和「生成总览」）。

## 怎么试玩

1. 打开工作树里的 `dist/Replay.app`（不要用 `/Applications/seesee.app`）。
2. 挑一条**已有本地字幕**的视频，展开右栏。
3. 页签应是「字幕 / 总览 / 笔记」。
4. **字幕搜索**：在字幕页输入词，命中句应变黄；点上下箭头在命中间滚动；点某一句应跳播放。
5. **解释 / 笔记**：在一句里拖选几个字，下方出现「解释」「存笔记」。
   - 无密钥：解释旁看到「未配置密钥」。
   - 有密钥：解释在当句下方出小气泡（一两句话）。存笔记后切到笔记页应能看到，点时间跳转，点垃圾桶删除。
6. **总览**：切到总览。无密钥时看到占位句。有密钥时点「生成总览」，等一会儿出现章节和金句。点条目应跳播放。关掉视频再打开，总览应直接在，不必再生成。
7. 密钥写法（本机已有 WatchQA 密钥则可跳过）：
   `defaults write com.mg.replay AnthropicAPIKey -string sk-…`
8. 总览和解释可改走本地 Gemini（WatchQA 仍走 Anthropic）：
   `defaults write com.mg.replay DigestProvider -string gemini`
   `defaults write com.mg.replay GeminiAPIKey -string …`
   或导出 `GEMINI_API_KEY`。改回：`defaults write com.mg.replay DigestProvider -string anthropic`。

Studio 上不要为了试玩启动应用。真机试玩交给老板或 Air。

## 四件功能完成度

| 功能 | 完成度 | 说明 |
|---|---|---|
| 字幕搜索 | 可用 | 命中、高亮、上下跳转、点句仍 seek。Enter 提交为下一个，Escape 清空。 |
| 选中解释 | 可用，手感偏生 | 必须拖选或双击选词才会出按钮；单击仍是跳播放。解释走文本请求，不截帧。 |
| 总览生成与缓存 | 可用 | 提示词写了「必须覆盖到片尾、最后一章要过 75%」；解析后若末章偏早，界面提示可重生成。未做自动重试。 |
| 时间戳笔记 | 可用 | 存、列表、跳转、删除、逐视频落盘。按创建时间新的在上。 |

## 自评：留

三分页值得留。搜索和总览是看长视频时立刻能感到的差别；笔记是解释的自然下落点，实现也薄。

不建议原样转正的部分：选中文字的手感（`NSTextView` 嵌在列表里），以及「自带章节」被收进总览之后，第一次打开总览更像一个生成入口，不像旧的章节目录。转正时要单独定：自带章节是总览的底，还是回到独立页签。

## 已知问题与转正前必改

1. **选中与点句抢手势**：单击空选会 seek，拖选才出「解释 / 存笔记」。列表里嵌 `NSTextView` 的高度和滚动可能发跳，要在真机上看。
2. **入口策略**：探索版无开关、三页直接可见。转正要定：是否跟 WatchQA 一样默认关，还是字幕搜索常开、总览/解释跟密钥走。
3. **密钥引导**：任务书明确留给 v0.2。现在只有「未配置密钥」一句话。
4. **总览质量**：只在本地检查末章是否过 75%，不自动重试；金句是否 3 到 5 条、时间戳是否真在字幕里，没有事后校验。
5. **自带章节去哪**：旧「章节」页签没有了。有自带章节的视频打开总览能看到它们，但和模型章节两套目录可能让人愣一下。
6. **页签命中区**：已加大到每签至少 26pt 高、横向内边距 12pt，字号 13；232 窄宽下三签加关闭钮同一行不换行不截断。
7. **删视频**：笔记和总览靠现有 `<uuid>.*` 前缀扫描清掉，没有 WatchQA 那种进程内删除标记。用户点存的瞬间不太会跟删除对撞，转正若要严格，可并进同一套 sidecar actor。
8. **未做真机点按**：按任务书未在 Studio 启动界面。构建产物已放在 `dist/Replay.app`。
